[project]
name = "offline-analysis-platform"
version = "0.1.0"
description = "Industrial AI offline intelligent analysis platform + online detection core"
requires-python = ">=3.11"

[tool.uv]
workspace = true

[tool.uv.sources]
seat-defect-core = { workspace = true }

[workspace]
members = ["seat_defect_core"]
